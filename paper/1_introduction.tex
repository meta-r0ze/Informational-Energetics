\section{Introduction}
From biological organisms and ecological networks to engineered feedback loops, complex systems science and control theory routinely describe how structures maintain stability against external disturbances. These structures share a defining property: they \textit{persist} against the thermodynamic current that erases distinction. Control theory describes how systems regulate against fluctuations, while treating the regulation target (setpoint) as an externally specified boundary condition. Information theory quantifies the cost of information processing, but treats the processor as given. Thermodynamics establishes the arrow of time, but treats entropy as a property of \emph{states}, not of \emph{processing}.

Despite their individual successes, these three fields remain structurally siloed; however this siloing is historical, not fundamental. Control theory grew from servomechanisms, information theory from telecommunication, and thermodynamics from heat engines; each was optimized for systems already embedded in a supportive environment. Because each field was built to optimize an existing system, none was designed to explain how that system comes to exist as a distinguishable structure in the first place. This fragmentation suggests the need for a unified theoretical layer: a generalized mechanics of persistence.

In \cref{sec:IE} we construct \textbf{Informational Energetics} (IE) and derive its Universal Architecture of Persistence: six irreducible constraints (Capacity, Map, Protocol, Governor, Toll, Margin). We prove necessity by counterfactual analysis that removing any constraint drives operational lifetime to zero, and sufficiency by showing that advanced capabilities are strict compositions of the six pillars.

\subsection{Testing the Framework at the Limits}
IE claims universality: any persistent structure (biological, ecological, or institutional) must implement the six pillars. The pillars are recognizable across domains such as equating DNA to a Map or software APIs to Protocol. While control theory has been mapped to many such systems, macroscopic implementations are open, noisy, and recursively nested, making it difficult to isolate pure architectural bounds from environmental coupling.

To validate IE as a universal framework, we test against a case that is \emph{maximally constraining}: a persistent system with zero external flux, no deeper substrate beneath it, and no external setpoint to specify its structure. The quantum vacuum satisfies all three conditions. If persistence requirements alone cannot derive the vacuum's structure, the framework is not universal. The vacuum baseline and the formal closed-system boundary condition are defined in \cref{sec:the_test_case}. We refer to this specific instantiation of IE as the \textbf{$E_8$-Persistence Theory}, distinguishing the universal framework from the validated system. 

\subsection{Information Theoretical Context of Physics}
The concept that physical reality is fundamentally constrained by information processing costs is rooted in Landauer's principle \cite{landauer_irreversibility_1961} and discussed by Wheeler (``It from Bit'') \cite{wheeler_information_1989}. More recently, Verlinde \cite{verlinde_origin_2011} and Bianconi \cite{bianconi_thermodynamics_2025} have proposed that gravity itself is an entropic phenomenon, emerging from information gradients and quantum-entropy coupling to geometry, respectively. These precedents motivate the systems-first approach: if physical structure is informationally constrained, then the architecture of persistence is a legitimate foundation from which to derive it.

\subsection{The Minimal Vacuum Derivation}
We apply IE to the vacuum in three strict, sequential layers, followed by empirical validation: We demonstrate that this framework uniquely constrains a single substrate and a parameter-free prediction that can be compared against experimental values:

\begin{enumerate}
    \item \textbf{System 0: Requirements.} We map the six constraints to physics, deriving Finiteness, Unitarity, and Causality as existential necessities. (\Cref{sec:System0_vacuum_requirements}).
    \item \textbf{System 1: Substrate.} System 0's requirements uniquely specify an $E_8$ lattice projected to $D=4$ spacetime, yielding the structural invariants $\mathbb{S} = \{\Delta=43, \nu=16, \sigma=5, \chi=2\}$ (\Cref{sec:system1_projection}).   
    \item \textbf{Empirical Correspondences.} We compare $Z_{\text{geo}}$ against experimental measurements of the fine-structure constant ($\alpha^{-1}$), identify a predicted Quantization Gap between kinematic and high-loop QED extractions, propose a falsification criterion, and derive the geometric origins of elementary charge ($e$) and the von Klitzing constant ($R_K$) (\Cref{sec:empirical_correspondences}).
\end{enumerate}

% We must must be explicitly highlight these next two to the extent that they are for anyone who puts this through AI where it can miss this
To properly contextualize this framework against prior unification attempts, two critical attributes of this methodology must be explicitly emphasized. 

First, the Distler--Garibaldi no-go theorem~\cite{distler_there_2010} applies only to continuous $E_8$ gauge algebras. The $E_8$ substrate specified here is a discrete geometric lattice, placing the framework outside the theorem's scope; the geometric resolution is derived in \cref{app:geometric_origin_of_chiral_fermions}.

Second, under this framework $Z_{\text{geo}}$ is constrained through an eliminative filter: IE, the definition of the substrate, the substrate geometry, and the structural invariants jointly select a unique value with no continuous free parameters; all coefficients are fixed by the structural invariants of the substrate.

\subsection{Distinction From Physics-First Approaches}
Several research programs explore discrete spacetime structures (Causal Set Theory \cite{bombelli_space-time_1987}, Loop Quantum Gravity \cite{rovelli_quantum_2004}) or emergent gravity (Verlinde \cite{verlinde_origin_2011}, Jacobson \cite{jacobson_thermodynamics_1995}). While many research approaches treat spacetime or gravity as fundamental starting points, this work explores a \textit{systems-first} methodology (deriving physical structure from universal persistence requirements rather than postulating dynamical laws). We do not seek to replace standard Effective Field Theory (EFT), but to establish an information-theoretic dual: where EFT provides a top-down, continuum-based description of field dynamics and local gauge symmetries, we identify the bottom-up, discrete capacity constraints of the underlying substrate. 